name: Deploy to AWS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    env:
      ENVIRONMENT: ${{ vars.ENVIRONMENT }}
      AWS_REGION: ${{ vars.AWS_REGION }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      BAMBOOHR_SUBDOMAIN: ${{ vars.BAMBOOHR_SUBDOMAIN }}
      TIME_OFF_OFFICE_CODE: ${{ vars.TIME_OFF_OFFICE_CODE }}
      EMPLOYEE_COUNTRY_FILTER: ${{ vars.EMPLOYEE_COUNTRY_FILTER }}
      CELEBRATIONS_CHANNEL_ID: ${{ vars.CELEBRATIONS_CHANNEL_ID }}
      BANK_HOLIDAYS_CHANNEL_ID: ${{ vars.BANK_HOLIDAYS_CHANNEL_ID }}
      OFFICE_CHANNEL_ID: ${{ vars.OFFICE_CHANNEL_ID }}
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - name: serverless deploy
        uses: serverless/github-action@v3.1
        with:
          args: deploy

